# 需求文档：close-to-tray

## 功能概述

关闭主窗口时不再直接退出进程（否则 MCP / Actions / 隧道一并断开）。弹出三按钮确认：取消、后台运行（藏到系统托盘，服务继续）、直接关闭（真退出）。托盘可恢复窗口或退出。对应 GitHub #8。

## 历史经验与坑（来自记忆库）

- **可复用经验**: 已有 `UI_RECREATING` + keepalive + `ExitRequested.prevent_exit`，证明「无 UI 窗口时进程可继续」。托盘隐藏是同一意图的用户可见形态。
- **必须规避的坑**:
  1. 退出路径禁止在 async worker 里嵌套 `block_on`（跨项目 shutdown 卡死经验）；托盘「退出」应直接 `app.exit` / 关闭窗口，不做重清理。
  2. `recreate_ui_webview` 销毁主窗时不得被「关窗→弹确认/hide」拦住；销毁后若原先是托盘隐藏态，重建后应继续隐藏，禁止自动抢焦点弹出。
  3. Windows 最小化几何哨兵坐标问题已在 0.1.32/33 修过；托盘用 `hide`/`show`，不要误用 minimize 语义混用。

## 术语定义

- **后台运行**: 隐藏主窗口，保留托盘图标与全部后台服务。
- **直接关闭**: 结束桌面进程（服务随进程退出）。
- **托盘**: Windows 通知区域 / macOS 菜单栏状态项。

---

## 范围边界

**In Scope（本次要做）**
- 点窗口关闭（X）→ 阻止默认退出 → 应用内三按钮确认对话框。
- 「取消」：无操作；「后台运行」：hide 主窗 + 确保托盘可用；「直接关闭」：真退出，不再二次确认。
- 同一应用进程内，用户选择「后台运行」后记住该选择；从托盘或二次启动唤起窗口后再次点关闭（X）时直接隐藏，不再重复确认。
- 关闭并重新启动应用进程后，进程内选择自动清空，首次点关闭（X）时重新展示确认对话框。
- 托盘：左键显示并聚焦主窗；菜单「显示窗口」「退出」（退出不弹确认）。
- `recreate_ui_webview` 与托盘隐藏态协同：重建后若应保持隐藏则继续 hide。
- Windows / macOS。
- 二次启动：若已有实例在托盘，尽量唤起主窗（Windows 单实例路径）。

**Out of Scope（本次不做）**
- 「关闭时总是后台 / 总是退出」的持久偏好设置（可后续加）。
- Linux 托盘正式支持与打包验证。
- 自定义托盘独立图标资源（可用默认窗口图标）。
- 关闭时优雅停止各工作区 runtime 的独立进度 UI。

---

## 需求列表

### FR-1: 关闭确认对话框

**优先级:** Must

#### 验收标准（EARS）

1. WHEN 用户点击主窗口关闭按钮 THEN 系统 SHALL 阻止默认退出并展示确认对话框。
2. WHEN 对话框展示 THEN 系统 SHALL 提供「取消」「后台运行」「直接关闭」三个操作。
3. WHEN 用户选择「取消」THEN 系统 SHALL 关闭对话框且窗口保持可见，进程不退出。

### FR-2: 后台运行到托盘

**优先级:** Must

#### 验收标准（EARS）

1. WHEN 用户选择「后台运行」THEN 系统 SHALL 隐藏主窗口，MCP / Actions / 隧道进程保持运行。
2. WHEN 进入后台运行 THEN 系统 SHALL 显示托盘图标。
3. WHEN 用户左键点击托盘图标或选择「显示窗口」THEN 系统 SHALL 显示并聚焦主窗口。

### FR-3: 真退出

**优先级:** Must

#### 验收标准（EARS）

1. WHEN 用户在确认框选择「直接关闭」THEN 系统 SHALL 退出应用进程且不再弹出确认。
2. WHEN 用户从托盘菜单选择「退出」THEN 系统 SHALL 退出应用进程且不再弹出确认。

### FR-4: 与 UI 重建协同

**优先级:** Must

#### 验收标准（EARS）

1. WHEN `recreate_ui_webview` 销毁并重建主窗 THEN 系统 SHALL 不因关窗拦截逻辑而失败或误弹确认。
2. IF 重建开始前主窗处于托盘隐藏态 THEN 重建完成后系统 SHALL 保持隐藏（不得自动弹出抢焦点）。
3. IF 重建开始前主窗可见 THEN 重建完成后系统 SHALL 恢复为可见可聚焦窗口（现有行为保留）。

### FR-5: 二次启动唤起（Windows）

**优先级:** Should

#### 验收标准（EARS）

1. WHEN 已有实例在运行（含托盘隐藏）且用户再次启动应用 THEN 系统 SHALL 尽量显示并聚焦已有主窗口，而不是静默失败无反馈。

### FR-6: 当前进程记忆关闭选择

**优先级:** Must

#### 验收标准（EARS）

1. WHEN 用户在当前进程首次选择「后台运行」 THEN 系统 SHALL 在进程内存中记住该选择。
2. WHILE 当前进程仍在运行 WHEN 主窗口被重新显示后用户再次点击关闭按钮 THEN 系统 SHALL 直接隐藏主窗口且不再展示确认对话框。
3. WHEN 应用进程完全退出并重新启动 THEN 系统 SHALL 清空上一次进程的关闭选择，并在首次点击关闭按钮时重新展示确认对话框。
4. WHEN 用户选择「取消」 THEN 系统 SHALL NOT 记忆该操作。

---

## 非功能需求

- **NFR-1**: 关窗确认与托盘操作不得阻塞或停止 MCP/隧道 supervisor。
- **NFR-2**: 「直接关闭」/托盘退出应在数秒内结束进程，避免「正在关闭」卡死。
- **NFR-3**: 对话框文案清晰说明后台运行会保持服务。
- **NFR-4**: 关闭选择不得写入配置文件、localStorage 或其它跨进程持久化介质。

## 依赖关系

- 依赖 Tauri 2 `tray-icon` feature 与现有 `tauri-plugin-dialog`（仅其它场景使用，本功能确认框为应用内 UI）。
- 依赖现有 `commands/ui_memory.rs` 的 `UI_RECREATING` / keepalive 机制，扩展隐藏态恢复。
- 依赖 Windows 现有 `CreateMutexW` 单实例逻辑，扩展唤起事件。
- 依赖前端根 layout（`+layout.svelte`）挂载全局确认框与 close-guard。

---

## 验收标准汇总

- [ ] 关窗弹出三按钮确认，取消无效退出
- [ ] 后台运行后隧道仍可用，托盘可恢复
- [ ] 直接关闭与托盘退出均真退出
- [ ] UI 重建在托盘隐藏态下不自动弹窗
- [ ] Windows 二次启动尽量唤起
- [ ] 同一进程选择后台运行后，后续关闭直接隐藏且不重复弹窗
- [ ] 完全退出并重启后，首次关闭恢复弹窗

## 检查清单

- [x] 已消化记忆库的历史经验，并逐条规避「历史坑」
- [x] 需求覆盖核心场景与边界场景
- [x] 每条需求有唯一 ID（FR-n），将在 design.md / tasks.md 中被引用
- [x] 验收标准使用 EARS 格式且可测
- [x] 范围边界（In/Out of Scope）明确
- [x] 依赖关系完整
